# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  thumbs_up: false
  user: <%= Fixtures.identify(:one) %>
  course: <%= Fixtures.identify(:one) %>

two:
  thumbs_up: true
  user: <%= Fixtures.identify(:two) %>
  course: <%=  Fixtures.identify(:two) %>
