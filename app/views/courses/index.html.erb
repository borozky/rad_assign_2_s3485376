<p id="notice"><%= notice %></p>

<h1>Courses</h1>


<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Prerequisite</th>
      <th>Locations</th>
      <th>Categories</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @courses.each do |course| %>
      <tr>
        <td><%= course.name %></td>
        <td><%= course.description %></td>
        <td><%= course.prerequisite %></td>
        <td>
          <%= course.locations.collect { |l| link_to(l.at, l) }.join(",").html_safe %>
        </td>
        <td>
          <%= course.categories.collect { |c| 
              link_to(c.name, c) }.join(",").html_safe 
          %>
        </td>
        <td><%= link_to 'Show', course %></td>
        <td><%= link_to 'Edit', edit_course_path(course) if course_editable?(course) %></td>
        <td><%= link_to 'Destroy', course, method: :delete, data: { confirm: 'Are you sure?' } if course_editable?(course) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Course', new_course_path %>
